# Наложение текста

Этот узел наносит текст поверх одного изображения или пакета изображений. Он создает текстовую накладку с настраиваемым размером шрифта, цветом, положением, выравниванием и необязательным контуром, а затем компонует текст поверх исходных изображений.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `изображения` | Входное изображение или пакет изображений для нанесения текста | IMAGE | Да | |
| `текст` | Текст для наложения на изображение (по умолчанию: ""). Поддерживает несколько строк; escape-последовательности `\n` и `\t` преобразуются в переводы строк и табуляции, а текст автоматически переносится, чтобы поместиться в ширину изображения. | STRING | Да | |
| `размер_шрифта` | Размер шрифта в процентах от высоты изображения (по умолчанию: 5.0) | FLOAT | Да | от 0.5 до 50.0 (шаг 0.5) |
| `цвет` | Цвет текста (по умолчанию: "#ffffff") | STRING | Да | |
| `позиция` | Вертикальное положение текста на изображении (по умолчанию: "top") | COMBO | Да | `"top"`<br>`"bottom"` |
| `выравнивание` | Горизонтальное выравнивание текста (по умолчанию: "left") | COMBO | Да | `"left"`<br>`"center"`<br>`"right"` |
| `контур` | Рисовать черный контур вокруг текста (по умолчанию: True) | BOOLEAN | Да | |
Примечание: если `text` пуст или содержит только пробелы, узел возвращает входные изображения без изменений. Одно и то же наложение текста применяется к каждому изображению в пакете. Для изображений с альфа-каналом (RGBA) текст компоновится с помощью альфа-смешивания source-over, поэтому существующая прозрачность сохраняется.

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-----------|------------|
| `изображения` | Входные изображения с наложенным поверх текстом | IMAGE |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TextOverlay/ru.md)

---
**Source fingerprint (SHA-256):** `b347f563fa26e098a310892f3e7fff41b83722800d67e5af9debad14fc9d01e7`
