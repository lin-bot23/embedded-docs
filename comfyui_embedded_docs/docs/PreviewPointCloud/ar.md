# معاينة سحابة النقاط

تتيح لك عقدة Preview Point Cloud عرض ملف سحابة نقاط ثلاثية الأبعاد في واجهة ComfyUI دون حفظه في دليل الإخراج الخاص بـ ComfyUI. فهي تحفظ سحابة النقاط في موقع مؤقت وتعرضها في نافذة معاينة ثلاثية الأبعاد، مع تمرير بيانات النموذج ومعلومات النموذج ومعلومات الكاميرا وأبعاد المعاينة للمعالجة اللاحقة.

## المدخلات

| المعامل | الوصف | نوع البيانات | مطلوب | النطاق |
|-----------|-------------|-----------|----------|-------|
| `نموذج_ثلاثي_الأبعاد` | ملف سحابة النقاط (.ply) | FILE3D | نعم | - |
| `معلومات_النموذج_ثلاثي_الأبعاد` | معلومات حول النموذج ثلاثي الأبعاد | LOAD3DMODELINFO | لا | - |
| `حالة_منفذ_العرض` | الحالة الحالية لمنفذ العرض ثلاثي الأبعاد | LOAD3D | نعم | - |
| `معلومات_الكاميرا` | معلومات الكاميرا للعرض ثلاثي الأبعاد | LOAD3DCAMERA | لا | - |
| `العرض` | عرض نافذة المعاينة (الافتراضي: 1024) | INT | نعم | 1 إلى 4096 |
| `الارتفاع` | ارتفاع نافذة المعاينة (الافتراضي: 1024) | INT | نعم | 1 إلى 4096 |

ملاحظة: `model_3d_info` و`camera_info` مدخلات اختيارية متقدمة. عندما تكون غير متصلة، تعود العقدة إلى القيم المقابلة المخزنة في `viewport_state`. يُكتب ملف سحابة النقاط إلى الدليل المؤقت لـ ComfyUI بدلاً من دليل الإخراج. هذه عقدة إخراج (طرفية)، لذلك تُستخدم أساسًا لعرض المعاينة في الواجهة.

## المخرجات

| اسم المخرج | الوصف | نوع البيانات |
|-------------|-------------|-----------|
| `نموذج_ثلاثي_الأبعاد` | بيانات نموذج سحابة النقاط | FILE3D |
| `معلومات_النموذج_ثلاثي_الأبعاد` | معلومات حول النموذج ثلاثي الأبعاد | LOAD3DMODELINFO |
| `معلومات_الكاميرا` | معلومات الكاميرا للعرض ثلاثي الأبعاد | LOAD3DCAMERA |
| `العرض` | عرض نافذة المعاينة | INT |
| `الارتفاع` | ارتفاع نافذة المعاينة | INT |

> تم إنشاء هذه الوثيقة بواسطة الذكاء الاصطناعي. إذا وجدت أي أخطاء أو لديك اقتراحات للتحسين، فلا تتردد في المساهمة! [تحرير على GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/PreviewPointCloud/ar.md)

---
**Source fingerprint (SHA-256):** `a0b13d9d5658343a6a7c25408d5e5cd9249c92264b76aa448a6553b370f4d782`
