# LTXVGeneratedKeyframesToGuides

## 概述

LTXV 生成关键帧到引导节点将早期阶段生成的关键帧作为冻结图像引导固定在后续画布上。它将关键帧解码为独立的帧，如果需要则调整大小，并使用噪声掩码为 0 写入，以防止进一步的降噪。记录的索引从生成它们的画布缩放到目标画布，您可以覆盖帧索引以显式设置位置。

## 输入

| 参数                 | 描述                                                                 | 数据类型 | 必需 | 范围 |
|---------------------------|-----------------------------------------------------------------------------|-----------|----------|-------|
| `正面条件` | 将关键帧固定为图像引导的正向条件。             | CONDITIONING | 是      |       |
| `负面条件` | 将关键帧固定为图像引导的负向条件。             | CONDITIONING | 是      |       |
| `vae`                     | 用于解码关键帧的 VAE 模型。                           | MODEL      | 是      |       |
| `潜空间` | 要添加引导的目标视频潜在视频，例如，时间上提升的。 | LATENT     | 是      |       |
| `关键帧` | LTXV 分离生成的关键帧的输出，其中包含每个关键帧生成的像素帧索引。 | LATENT     | 是      |       |
| `强度` | 引导强度。1.0 是硬固定；较低值会放松它。                   | FLOAT      | 是      | 0.0 - 10.0 |
| `override_frame_indices` | 可选——在这些像素帧上固定，而不是记录的（或自动缩放的）位置。每个关键帧提供一个索引。留空以重用记录的位置，或在目标画布长度不同时（例如，在时间 x2 之后）缩放它们。 | STRING    | 否       |       |

## 输出

| 输出名称 | 描述                                                                 | 数据类型 |
|-------------|-----------------------------------------------------------------------------|-----------|
| `正面条件`  | 将关键帧固定为图像引导的正向条件。             | CONDITIONING |
| `负面条件`  | 将关键帧固定为图像引导的负向条件。             | CONDITIONING |
| `潜空间`    | 添加了关键帧作为冻结引导的目标视频潜在视频。               | LATENT     |

## 注意事项

- `strength` 参数控制关键帧作为引导的固定强度。1.0 创建硬固定，而较低值会放松固定。
- `override_frame_indices` 参数允许您指定关键帧应固定的确切像素帧。如果留空，节点将使用记录的位置或必要时缩放它们。
- 节点假定 `keyframes` 潜在包含每个关键帧的像素帧索引。如果不是这种情况，节点将引发 `ValueError`。
- 节点仅支持批量大小为 1。每个引导从一个图像编码，因此它不能在批量元素之间不同。
- 如果 `latent` 输入中的 `samples` 张量不是 5D 张量或批量大小不是 1，节点将引发 `ValueError`。
- 如果 `keyframes` 输入中的 `samples` 张量不是 5D 张量或批量大小不是 1，节点将引发 `ValueError`。
- 如果 `keyframes` 输入中的 `samples` 张量的形状在调整大小后不匹配 `latent` 输入中的 `samples` 张量的形状，节点将引发 `ValueError`。
- 如果 `strength` 参数超出 0.0 到 10.0 的范围，节点将引发 `ValueError`。
- 如果 `override_frame_indices` 参数不是逗号分隔的整数列表或索引数量与关键帧数量不匹配，节点将引发 `ValueError`。
- 如果 `override_frame_indices` 参数中的任何索引超出目标画布中像素帧的范围，节点将引发 `ValueError`。
- 如果 `override_frame_indices` 参数中的最大索引大于目标画布中像素帧的数量，节点将引发 `ValueError`。

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVGeneratedKeyframesToGuides/zh.md)

---
**Source fingerprint (SHA-256):** `b5dbf302fad5a7ffd3522d468d1a51b993145d90277592058315499f08e17e7b`
