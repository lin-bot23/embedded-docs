# Tripo: تکمیل بخش‌های مش

قطعات یک مدل سه‌بعدی تقسیم‌بندی‌شده را تکمیل می‌کند و نواحی گم‌شده یا آسیب‌دیده مش را ترمیم می‌کند. شناسه وظیفه (Task ID) یک نتیجه تقسیم‌بندی مش Tripo را می‌گیرد، درخواست کار تکمیل را از Tripo ارسال می‌کند و منتظر می‌ماند تا تمام شود. می‌توانید به‌صورت اختیاری کار را به نام‌های قطعه مشخصی محدود کنید. مدل تکمیل‌شده به‌صورت یک فایل GLB بازگردانده می‌شود.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|-----------|-------------|-----------|----------|-------|
| `segment_task_id` | شناسه وظیفه یک وظیفه تقسیم‌بندی مش Tripo. قطعات مدل تقسیم‌بندی‌شده از این وظیفه تکمیل می‌شوند. خروجی SEGMENT_TASK_ID یک گره تقسیم‌بندی مش Tripo قبلی را وصل کنید. | SEGMENT_TASK_ID | بله | یک شناسه وظیفه |
| `part_names` | نام قطعات جداشده با کاما برای تکمیل. خالی همه قطعات را تکمیل می‌کند. پیش‌فرض: رشته خالی. فاصله‌های اضافی اطراف نام‌ها حذف می‌شوند و نام‌های تکراری نادیده گرفته می‌شوند. | STRING | خیر | متن آزاد یا خالی |

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
|-------------|-------------|-----------|
| `model_file` | نام فایل مدل تکمیل‌شده. این خروجی فقط برای سازگاری با نسخه‌های قبلی وجود دارد. | STRING |
| `task_id مدل` | شناسه وظیفه کار تکمیل مش Tripo. می‌تواند به‌عنوان ورودی توسط سایر گره‌های Tripo که یک شناسه وظیفه مدل انتظار دارند استفاده شود. | MODEL_TASK_ID |
| `GLB` | مدل سه‌بعدی تکمیل‌شده با قطعات ترمیم‌شده، که به‌صورت فایل GLB دانلود می‌شود. | GLB |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TripoMeshCompleteNode/fa.md)

---
**Source fingerprint (SHA-256):** `c5709231fa2e33e6f3c9b25669acca1d4ae9adb882b90210d703aeddc0d11ecc`
