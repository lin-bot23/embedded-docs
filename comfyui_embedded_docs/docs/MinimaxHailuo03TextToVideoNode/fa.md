# تبدیل متن به ویدیو MiniMax H3

این گره با استفاده از خانواده مدل‌های MiniMax H3 شامل MiniMax H3، MiniMax H3 Max و MiniMax H3 Max Turbo، از روی یک متن prompt ویدیو تولید می‌کند. شما مدل را انتخاب کرده، متن prompt را وارد می‌کنید و تنظیماتی نظیر رزولوشن، نسبت ابعاد و مدت زمان را تنظیم می‌نمایید. این گره درخواست را به API شرکت MiniMax ارسال کرده، پس از تکمیل فرآیند تولید، ویدیوی نهایی را بازگرداند.

## ورودی‌ها

### ورودی‌های مشترک

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|-----------|-------------|-----------|----------|-------|
| `model` | مدل مورد استفاده برای تولید ویدیو (پیش‌فرض: "MiniMax H3"). انتخاب یک مدل، تنظیمات خاص آن مدل را که در بخش‌های زیر توضیح داده شده است نیز نمایش می‌دهد. | DYNAMIC_COMBO | بله | "MiniMax H3"<br>"MiniMax H3 Max"<br>"MiniMax H3 Max Turbo" |
| `seed` | seed تصادفی. درخواست یکسان با seed یکسان نتایج مشابهی می‌دهد، اما یکسان بودن آن‌ها تضمین نمی‌شود (پیش‌فرض: 42). | INT | بله | 0 تا 4294967295 |
| `watermark` | مشخص می‌کند که آیا واترمارک AIGC به ویدیو اضافه شود یا خیر (پیش‌فرض: false). وقتی فعال باشد، فقط مدل "MiniMax H3" پشتیبانی می‌شود. | BOOLEAN | خیر | true<br>false |

### ورودی‌های MiniMax H3

این تنظیمات زمانی نمایش داده می‌شوند که مدل "MiniMax H3" انتخاب شده باشد.

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|-----------|-------------|-----------|----------|-------|
| `prompt` | متن prompt برای تولید ویدیو. باید حداقل شامل یک کاراکتر غیر از فاصله باشد. | STRING | بله | هر متنی |
| `resolution` | رزولوشن ویدیوی خروجی. | COMBO | بله | "768P"<br>"2K" |
| `ratio` | نسبت ابعاد ویدیوی خروجی (پیش‌فرض: "16:9"). | COMBO | بله | "16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9" |
| `duration` | مدت زمان ویدیوی خروجی بر حسب ثانیه (4-15) (پیش‌فرض: 5). | INT | بله | 4 تا 15 |

### ورودی‌های MiniMax H3 Max و MiniMax H3 Max Turbo

این تنظیمات بین مدل‌های "MiniMax H3 Max" و "MiniMax H3 Max Turbo" مشترک هستند و زمانی نمایش داده می‌شوند که هر یک از این مدل‌ها انتخاب شود.

| پارامتر | توضیحات | نوع داده | الزامی | محدوده |
|-----------|-------------|-----------|----------|-------|
| `prompt` | متن prompt برای تولید ویدیو. باید حداقل شامل یک کاراکتر غیر از فاصله باشد و می‌تواند تا 50,000 کاراکتر طول داشته باشد. | STRING | بله | تا 50000 کاراکتر |
| `resolution` | رزولوشن ویدیوی خروجی (پیش‌فرض: "768P"). | COMBO | بله | "480P"<br>"768P" |
| `ratio` | نسبت ابعاد ویدیوی خروجی (پیش‌فرض: "16:9"). | COMBO | بله | "16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9" |
| `duration` | مدت زمان ویدیوی خروجی بر حسب ثانیه (5-15) (پیش‌فرض: 5). | INT | بله | 5 تا 15 |
| `prompt_expansion_mode` | میزان تلاش صرف شده برای بازنویسی prompt قبل از تولید (پیش‌فرض: "balanced"). | COMBO | بله | "balanced"<br>"quality" |

### نکات

- برای تمام مدل‌ها، prompt باید حداقل شامل یک کاراکتر غیر از فاصله باشد.
- تنظیم `watermark` فقط توسط "MiniMax H3" پشتیبانی می‌شود. فعال کردن آن با "MiniMax H3 Max" یا "MiniMax H3 Max Turbo" باعث خطا می‌شود.
- مدل‌های "MiniMax H3 Max" و "MiniMax H3 Max Turbo" prompt را به 50,000 کاراکتر محدود می‌کنند.
- محدودیت‌های رزولوشن و مدت زمان به مدل انتخاب شده بستگی دارد: "MiniMax H3" از رزولوشن‌های "768P" و "2K" و ویدیوهای 4 تا 15 ثانیه‌ای پشتیبانی می‌کند، در حالی که "MiniMax H3 Max" و "MiniMax H3 Max Turbo" از رزولوشن‌های "480P" و "768P" و ویدیوهای 5 تا 15 ثانیه‌ای پشتیبانی می‌کنند.
- قیمت تخمینی نمایش داده شده برای این گره بر اساس مدل، رزولوشن و مدت زمان انتخاب شده محاسبه می‌شود.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
|-------------|-------------|-----------|
| `VIDEO` | ویدیوی تولید شده از روی متن prompt ارائه شده. | VIDEO |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MinimaxHailuo03TextToVideoNode/fa.md)

---
**Source fingerprint (SHA-256):** `4d3de190d18de4370aff878279755e881841d2ada28320a7c1d7c52061071c05`
