# 规格化图像

此节点通过数学归一化过程调整输入图像的像素值。它从每个像素中减去指定的均值，然后将结果除以指定的标准差。这是为其他机器学习模型准备图像数据时常见的预处理步骤。

## 输入

| 参数 | 描述 | 数据类型 | 是否必填 | 范围 |
| --- | --- | --- | --- | --- |
| `image` | 要进行归一化的输入图像。 | IMAGE | 是 | - |
| `平均` | 归一化使用的均值（默认值：0.5）。 | FLOAT | 否 | 0.0 - 1.0 |
| `标准差` | 归一化使用的标准差（默认值：0.5）。 | FLOAT | 否 | 0.001 - 1.0 |

注意：当输入图像包含 alpha（透明度）通道时，该通道不会被归一化。由于 alpha 存储的是透明度而非颜色信息，因此输出中保持其不变。

## 输出

| 输出名称 | 描述 | 数据类型 |
| --- | --- | --- |
| `图像` | 应用归一化过程后得到的图像。 | IMAGE |

> 本文档由 AI 生成。如果您发现任何错误或有改进建议，欢迎贡献！ [在 GitHub 上编辑](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/NormalizeImages/zh.md)

---
**Source fingerprint (SHA-256):** `30c0587265754842ff7d1e5f339fc934b58d59bb3ba18716c2a1f9679f2d561d`
