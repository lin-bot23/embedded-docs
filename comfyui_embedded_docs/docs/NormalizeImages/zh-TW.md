# 標準化圖片

```markdown
# 正規化圖像顏色

此節點使用數學正規化過程調整輸入圖像的像素值。它從每個像素中減去指定的平均值，然後將結果除以指定的標準差。這是一個常見的預處理步驟，用於為其他機器學習模型準備圖像數據。如果輸入圖像有 alpha 通道，則 alpha 通道將保持不變，以保留透明度。

## 概述

 Normalize Images 節點透過基於平均值和標準差調整輸入圖像的像素值來正規化其顏色。此過程用於在應用機器學習算法之前標準化圖像數據。

## 輸入

| 參數 | 描述 | 資料類型 | 必需 | 范圍 |
|-----------|-------------|-----------|----------|-------|
| `image`   | 要正規化的輸入圖像。 | IMAGE | 是 | - |
| `平均值` | 正規化的平均值。 | FLOAT | 否 | 0.0 - 1.0 (預設: 0.5) |
| `標準差` | 正規化的標準差。 | FLOAT | 否 | 0.001 - 1.0 (預設: 0.5) |

`mean` 和 `std` 參數用於正規化輸入圖像的像素值。兩個參數的預設值均設為 0.5，這是一個常見的正規化選擇。

## 輸出

| 輸出名 | 描述 | 資料類型 |
|-------------|-------------|-----------|
| `圖片`     | 應用正規化過程後的結果圖像。 | IMAGE |

Normalize Images 節點的輸出是正規化後的圖像。像素值將根據指定的平均值和標準差進行調整，並保留 alpha 通道（如果存在）。

## 注意

Normalize Images 節點設計用於處理任何批次的圖像大小，使其適合批處理任務。
```

> 本文檔由 AI 生成。如果您發現任何錯誤或有改進建議，歡迎貢獻！ [在 GitHub 上編輯](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/NormalizeImages/zh-TW.md)

---
**Source fingerprint (SHA-256):** `30c0587265754842ff7d1e5f339fc934b58d59bb3ba18716c2a1f9679f2d561d`
