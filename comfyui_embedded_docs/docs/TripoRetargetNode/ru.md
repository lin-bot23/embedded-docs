# Tripo: Перепривязка ригнутой модели

Узел TripoRetargetNode применяет предустановленную анимацию к существующей 3D-модели с ригом. Он принимает ID задачи модели, которая ранее была оснащена ригом, отправляет запрос на ретаргетинг в API Tripo и скачивает итоговый анимированный файл. Анимированная модель может быть возвращена в формате GLB или FBX, с опциональной геометрией меша и опциональным воспроизведением на месте.

## Входы

| Параметр | Описание | Тип данных | Обязательный | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `идентификатор_задачи_исходной_модели` | ID задачи ранее оснащённой ригом 3D-модели, которую нужно ретаргетировать. Указанная задача должна быть задачей риггинга. | RIG_TASK_ID | Да | - |
| `анимация` | Предустановленная анимация для применения к модели с ригом. Анимации `preset:*` работают с обеими моделями рига. Анимации `preset:biped:*` созданы для ригов из модели v1.0-20240301; риг v2.5 принимает только chop, climb, dive, fall, hurt, idle, jump, run, shoot, slash, turn и walk. | COMBO | Да | `"preset:idle"`<br>`"preset:walk"`<br>`"preset:run"`<br>`"preset:dive"`<br>`"preset:climb"`<br>`"preset:jump"`<br>`"preset:slash"`<br>`"preset:shoot"`<br>`"preset:hurt"`<br>`"preset:fall"`<br>`"preset:turn"`<br>`"preset:quadruped:walk"`<br>`"preset:hexapod:walk"`<br>`"preset:octopod:walk"`<br>`"preset:serpentine:march"`<br>`"preset:aquatic:march"`<br>плюс дополнительные опции `"preset:biped:*"`, показанные в интерфейсе |
| `out_format` | Формат выходного файла; результат поступает на соответствующий выход. (по умолчанию: glb) | COMBO | Нет | `"glb"`<br>`"fbx"` |
| `export_with_geometry` | Включать меш в экспорт; при выключении экспортируется только анимированный скелет. (по умолчанию: True) | BOOLEAN | Нет | True<br>False |
| `animate_in_place` | Воспроизводить анимацию на месте, без смещения корня. (по умолчанию: False) | BOOLEAN | Нет | True<br>False |
| `auth_token_comfy_org` | Токен аутентификации для доступа к API Comfy.org (скрытый параметр). | AUTH_TOKEN_COMFY_ORG | Нет | - |
| `api_key_comfy_org` | API-ключ для доступа к сервису Comfy.org (скрытый параметр). | API_KEY_COMFY_ORG | Нет | - |
| `unique_id` | Уникальный идентификатор для отслеживания операции (скрытый параметр). | UNIQUE_ID | Нет | - |

Примечание. Анимации в группе `preset:*` работают с обеими моделями рига. Анимации в группе `preset:biped:*` созданы для ригов из модели v1.0-20240301; риг v2.5 принимает только chop, climb, dive, fall, hurt, idle, jump, run, shoot, slash, turn и walk. Если указанный риг был создан по спецификации Mixamo и с версией модели, начинающейся с `v1.0`, вызов ретаргетинга завершается ошибкой. Запрошенный формат вывода должен быть GLB или FBX; если сервис возвращает файл любого другого типа, узел выдаёт ошибку.

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-------------|-----------|
| `файл модели` | Сгенерированный файл анимированной 3D-модели (только для обратной совместимости). | STRING |
| `task_id ретаргетинга` | ID задачи для отслеживания операции ретаргетинга. | RETARGET_TASK_ID |
| `GLB` | Анимированная 3D-модель в формате GLB. Заполняется, когда `out_format` имеет значение glb. | FILE3DGLB |
| `FBX` | Анимированная 3D-модель в формате FBX. Заполняется, когда `out_format` имеет значение fbx. | FILE3DFBX |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TripoRetargetNode/ru.md)

---
**Source fingerprint (SHA-256):** `4814858b940ece13f85010ff81fcdac0258fe8550aebd914be2613e8f40c0e5a`
