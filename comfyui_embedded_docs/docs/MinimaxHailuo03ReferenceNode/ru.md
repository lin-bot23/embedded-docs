# MiniMax H3: Референс в видео

Этот узел генерирует видео с использованием моделей MiniMax H3 на основе эталонных изображений, видео и аудио. На эталоны ссылаются в промпте по порядку их подключения: «Image 1», «Image 2», «Video 1», «Audio 1» и т. д. Доступны две модели: «MiniMax H3» и «MiniMax H3 Max».

## Входы

### Общие входы

| Параметр | Описание | Тип данных | Обязательность | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `model` | Модель для генерации видео (по умолчанию: "MiniMax H3"). Выбор «MiniMax H3» предоставляет входы генерации и эталонные входы MiniMax H3 ниже. Выбор «MiniMax H3 Max» предоставляет входы генерации и эталонные входы MiniMax H3 Max ниже. | DYNAMIC_COMBO | Да | "MiniMax H3"<br>"MiniMax H3 Max" |
| `seed` | Случайный сид. Одинаковый запрос с одним и тем же сидом даёт похожие, но не гарантированно идентичные результаты (по умолчанию: 42). | INT | Да | 0 до 4294967295 |
| `watermark` | Добавлять ли к видео AIGC-водяной знак (по умолчанию: false). Поддерживается только моделью MiniMax H3. | BOOLEAN | Нет | true<br>false |

### Входы MiniMax H3

Эти входы доступны, когда в качестве модели выбрана «MiniMax H3».

| Параметр | Описание | Тип данных | Обязательность | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `prompt` | Текстовый промпт для генерации видео. На эталонные медиа можно ссылаться по порядку их подключения, например «Image 1», «Image 2», «Video 1» или «Audio 1». | STRING | Да | минимум 1 символ |
| `resolution` | Разрешение выходного видео (по умолчанию: "768P"). | COMBO | Да | "768P"<br>"2K" |
| `ratio` | Соотношение сторон выходного видео (по умолчанию: "adaptive"). | COMBO | Да | "adaptive"<br>"16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9" |
| `duration` | Длительность выходного видео в секундах (по умолчанию: 5). | INT | Да | 4 до 15 |

### Входы MiniMax H3 Max

Эти входы доступны, когда в качестве модели выбрана «MiniMax H3 Max».

| Параметр | Описание | Тип данных | Обязательность | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `prompt` | Текстовый промпт для генерации видео. На эталонные медиа можно ссылаться по порядку их подключения, например «Image 1», «Image 2», «Video 1» или «Audio 1». | STRING | Да | от 1 до 50000 символов |
| `resolution` | Разрешение выходного видео (по умолчанию: "768P"). | COMBO | Да | "480P"<br>"768P" |
| `ratio` | Соотношение сторон выходного видео (по умолчанию: "adaptive"). | COMBO | Да | "adaptive"<br>"16:9"<br>"4:3"<br>"1:1"<br>"3:4"<br>"9:16"<br>"21:9" |
| `duration` | Длительность выходного видео в секундах (по умолчанию: 5). | INT | Да | 5 до 15 |
| `prompt_expansion_mode` | Сколько усилий затрачивается на переписывание промпта перед генерацией (по умолчанию: "balanced"). | COMBO | Да | "balanced"<br>"quality" |
| `reference_detail` | Уровень детализации, с которым отправляются эталонные изображения. «high» отправляет их в максимальном размере, который использует модель (до короткой стороны 2048 пикселей); «standard» уменьшает их до не более 2048x1024, чтобы снизить стоимость эталонов (по умолчанию: "standard"). | COMBO | Да | "high"<br>"standard" |

### Эталонные входы

Эти эталонные входы являются общими для обеих моделей. Каждый из них — расширяемый слот.

| Параметр | Описание | Тип данных | Обязательность | Диапазон |
|-----------|-------------|-----------|----------|-------|
| `reference_images` | Расширяемый слот: подключите до 9 элементов (`image_1`...`image_9`). Эталонные изображения объекта или стиля, на которые в промпте ссылаются как «Image 1»..«Image 9» в порядке подключения. До 9 изображений. | IMAGE | Нет | от 0 до 9 изображений |
| `reference_videos` | Расширяемый слот: подключите до 3 элементов (`video_1`...`video_3`). Эталонные видео движения или сцены, на которые в промпте ссылаются как «Video 1»..«Video 3» в порядке подключения. До 3 видео, каждое длительностью 2–15 секунд, суммарно 15 секунд. | VIDEO | Нет | от 0 до 3 видео |
| `reference_audios` | Расширяемый слот: подключите до 3 элементов (`audio_1`...`audio_3`). Аудиоэталоны, на которые в промпте ссылаются как «Audio 1»..«Audio 3» в порядке подключения. До 3 клипов, каждый длительностью 2–15 секунд, суммарно 15 секунд. Нельзя использовать без эталонного изображения или видео. | AUDIO | Нет | от 0 до 3 клипов |

### Ограничения параметров

- Требуется как минимум одно эталонное изображение или одно эталонное видео. Только эталонное аудио не принимается.
- Каждое эталонное изображение должно иметь соотношение сторон приблизительно от 0,4 до 2,5 (от 2:5 до 5:2) и минимальную ширину и высоту 256 пикселей.
- Каждое эталонное видео должно иметь длительность от 2 до 15 секунд и частоту кадров от 23,976 до 60 FPS. Суммарная длительность всех эталонных видео не может превышать 15 секунд.
- Каждый эталонный аудиоклип должен иметь длительность от 2 до 15 секунд. Суммарная длительность всех эталонных аудиоклипов не может превышать 15 секунд.
- Когда выбрана модель «MiniMax H3 Max», параметр `watermark` должен быть отключен.
- Когда выбрана модель «MiniMax H3 Max», общее количество эталонных файлов (изображений, видео и аудио вместе) не может превышать 12.

## Выходы

| Имя выхода | Описание | Тип данных |
|-------------|-------------|-----------|
| `video` | Сгенерированное видео. | VIDEO |

> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/MinimaxHailuo03ReferenceNode/ru.md)

---
**Source fingerprint (SHA-256):** `b77eedb1f7757e60518c04484f1cc24c27cf6886b3ae31c15207ea49fd436a73`
