# TripoTextToModelNodeV2

با استفاده از سرویس Tripo یک مدل سه‌بعدی از روی توضیح متنی تولید می‌کند. این گره `prompt` و تنظیمات را به Tripo می‌فرستد، منتظر می‌ماند تا وظیفه تولید کامل شود، و فایل سه‌بعدی نهایی را همراه با شناسه وظیفه برمی‌گرداند.

## ورودی‌ها

| پارامتر | توضیحات | نوع داده | الزامی | بازه |
|-----------|-------------|-----------|----------|-------|
| `prompt` | توضیح متنی مدلی که باید تولید شود. نباید خالی باشد. | STRING | بله | متن چندخطی |
| `negative_prompt` | متنی که توصیف می‌کند چه چیزی نباید در مدل تولیدشده ظاهر شود. حداکثر 255 کاراکتر. | STRING | خیر | متن چندخطی، حداکثر 255 کاراکتر |
| `model_version` | نسخه مدل Tripo که برای تولید استفاده می‌شود (پیش‌فرض: `v3.1_20260211`). | COMBO | خیر | فهرست نسخه‌های مدل Tripo پشتیبانی‌شده |
| `texture` | نقشه‌های بافت را تولید می‌کند. خاموش بودن آن هندسه بدون بافت برمی‌گرداند و `pbr` را نادیده می‌گیرد (پیش‌فرض: True). | BOOLEAN | خیر | True<br>False |
| `pbr` | نقشه‌های متریال PBR (رنگ پایه، متالیک، زبری، نرمال). به `texture` نیاز دارد (پیش‌فرض: True). | BOOLEAN | خیر | True<br>False |
| `image_seed` | مقدار seed برای تولید تصویر (پیش‌فرض: 42). | INT | خیر | 0 تا 2147483647 |
| `model_seed` | مقدار seed برای تولید مدل (پیش‌فرض: 42). | INT | خیر | 0 تا 2147483647 |
| `texture_seed` | مقدار seed برای تولید بافت (پیش‌فرض: 42). | INT | خیر | 0 تا 2147483647 |
| `texture_quality` | سطح جزئیات بافت (پیش‌فرض: "standard"). "detailed" = بافت‌های HD، "extreme" = بافت‌های 8K Ultra. | COMBO | خیر | `"standard"`<br>`"detailed"`<br>`"extreme"` |
| `face_limit` | حداکثر تعداد وجه. مقدار -1 باعث می‌شود Tripo به‌صورت تطبیقی انتخاب کند (حدود 1.4M وجه در v3.x استاندارد، 2M در حالت detailed). Tripo بی‌سروصدا محدود می‌کند: v2.5 در 500,000، مش‌های quad در 150,000 (پیش‌فرض: -1). | INT | خیر | -1 تا 2000000 |
| `quad` | خروجی مش چهارضلعی (quad). Tripo مش‌های quad را به‌صورت FBX ارائه می‌دهد، بنابراین نتیجه در خروجی FBX می‌آید و خروجی GLB خالی می‌ماند (پیش‌فرض: False). | BOOLEAN | خیر | True<br>False |
| `geometry_quality` | سطح جزئیات هندسه (پیش‌فرض: "standard"). | COMBO | خیر | `"standard"`<br>`"detailed"` |
| `smart_low_poly` | مش کم‌پالیگان با توپولوژی تمیز و به سبک دست‌ساز (500-20,000 وجه، quad 500-10,000). برای سوژه‌های ساده بهترین است؛ ممکن است سوژه‌های پیچیده شکست بخورند (پیش‌فرض: False). | BOOLEAN | خیر | True<br>False |
| `auto_size` | مدل‌های دارای بافت را به اندازه واقعی‌شان بر حسب متر مقیاس‌دهی می‌کند. Tripo اندازه را به‌عنوان ترنسفورم صحنه مدل ذخیره می‌کند و هنگام تبدیل، ریگ‌بندی یا ری‌تارگت مدل آن را bake می‌کند؛ بدون بافت نادیده گرفته می‌شود (پیش‌فرض: True). | BOOLEAN | خیر | True<br>False |

### یادداشت‌ها

- `prompt` الزامی است و نمی‌تواند خالی یا فقط شامل فاصله باشد.
- وقتی `texture` روی False تنظیم شود، `pbr` به‌اجبار خاموش می‌شود و `auto_size` اثری ندارد.
- وقتی `smart_low_poly` فعال است و `face_limit` برابر -1 نیست، `face_limit` باید برای مش‌های مثلثی بین 500 و 20,000 باشد، یا وقتی `quad` فعال است بین 500 و 10,000 باشد.
- با فعال بودن `quad`، Tripo یک فایل FBX برمی‌گرداند؛ بنابراین خروجی `GLB` خالی می‌ماند و خروجی `FBX` پر می‌شود.

## خروجی‌ها

| نام خروجی | توضیحات | نوع داده |
|-------------|-------------|-----------|
| `model task_id` | شناسه وظیفه تولید Tripo که مدل را تولید کرده است. | MODEL_TASK_ID |
| `GLB` | مدل تولیدشده در قالب GLB. وقتی `quad` فعال باشد خالی است. | FILE3D_GLB |
| `FBX` | مدل تولیدشده در قالب FBX. فقط وقتی `quad` فعال باشد پر می‌شود. | FILE3D_FBX |

> این مستند با هوش مصنوعی تهیه شده است. اگر خطایی دیدید یا پیشنهادی برای بهبود دارید، خوشحال می‌شویم مشارکت کنید! [ویرایش در GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/TripoTextToModelNodeV2/fa.md)

---
**Source fingerprint (SHA-256):** `8af7044188c6dbb87d23298bf7b99fe826bdc7bd7ba0948db2066887274faa00`
